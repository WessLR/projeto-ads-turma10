from Ouvidoria import *

conexao = criarConexao('localhost', 'root', 'LeonardoADS26', 'Ouvidoria')


while True:
    print("Bem vindo ao nosso sistema de Ouvidoria!")
    print("Digite a opção desejada: \n 1) Reclamações \n 2) Denúncias \n 3) Sugestões \n 4) Elogios \n 5) Solicitações \n 6) Relatórios \n 7) Sair")
    
    try:
        opcao = int(input("Escolha uma opção: "))
    except ValueError:
        print('Erro: Digite apenas números!')
        continue


    if opcao == 1:

        while True:
            print("Opções de Reclamação: ")
            print(" \n 1) Registrar Reclamação \n 2) Listar Reclamações \n 3) Pesquisar Reclamação \n 4) Atualizar Reclamação \n 5) Remover Reclamação \n 6) Voltar")

            try:
                opcao = int(input("Escolha uma opção: "))
            except ValueError:
                print('Erro: Digite apenas números!')
                continue

            if opcao == 1:

                registrar(conexao, "Reclamação")       #ele vai chamar a função registrar e passar a conexão e a categoria "Reclamação" como argumentos

            elif opcao == 2:

                listar(conexao, "Reclamação")
            
            elif opcao == 3:

                pesquisar(conexao, "Reclamação")

            elif opcao == 4:
                
                atualizar(conexao, "Reclamação")

            elif opcao == 5:

                remover(conexao, "Reclamação")
            
            
            elif opcao == 6:
                break

            else:
                print("Opção inválida. Por favor, escolha uma opção válida.")

    elif opcao == 2:

        while True:

            print("Opções de Denúncia: ")
            print(" \n 1) Registrar Denúncia \n 2) Listar Denúncias \n 3) Pesquisar Denúncia \n 4) Atualizar Denúncia \n 5) Remover Denúncia \n 6) Voltar")

            try:
                opcao = int(input("Escolha uma opção: "))
            except ValueError:
                print('Erro: Digite apenas números!')
                continue

            if opcao == 1:

                registrar(conexao, "Denúncia")

            elif opcao == 2:

                listar(conexao, "Denúncia")

            elif opcao == 3:

                pesquisar(conexao, "Denúncia")
            
            elif opcao == 4:

                atualizar(conexao, "Denúncia")

            elif opcao == 5:

                remover(conexao, "Denúncia")
            
            elif opcao == 6:
                break

    elif opcao == 3:
        
        while True:

            print("Opções de Sugestão: ")
            print(" \n 1) Registrar Sugestão \n 2) Listar Sugestões \n 3) Pesquisar Sugestão \n 4) Atualizar Sugestão \n 5) Remover Sugestão \n 6) Voltar")

            try:
                opcao = int(input("Escolha uma opção: "))
            except ValueError:
                print('Erro: Digite apenas números!')
                continue

            if opcao == 1:
        
                registrar(conexao, "Sugestão")

            elif opcao == 2:

                listar(conexao, "Sugestão")

            elif opcao == 3:

                pesquisar(conexao, "Sugestão")

            elif opcao == 4:

                atualizar(conexao, "Sugestão")

            elif opcao == 5:

                remover(conexao, "Sugestão")

            elif opcao == 6:
                break

    elif opcao == 4:

        while True:

            print("Opções de Elogio: ")
            print(" \n 1) Registrar Elogio \n 2) Listar Elogios \n 3) Pesquisar Elogio \n 4) Atualizar Elogio \n 5) Remover Elogio \n 6) Voltar")
            try:
                opcao = int(input("Escolha uma opção: "))

            except ValueError:
                print('Erro: Digite apenas números!')
                continue

            
            if opcao == 1:

                registrar(conexao, "Elogio")

            elif opcao == 2:

                listar(conexao, "Elogio")

            elif opcao == 3:

                pesquisar(conexao, "Elogio")

            elif opcao == 4:

                atualizar(conexao, "Elogio")

            elif opcao == 5:

                remover(conexao, "Elogio")

            elif opcao == 6:
                break       
            

    elif opcao == 5:
        
        while True:
            print("Opções de Solicitação: ")
            print(" \n 1) Registrar Solicitação \n 2) Listar Solicitações \n 3) Pesquisar Solicitação \n 4) Atualizar Solicitação \n 5) Remover Solicitação \n 6) Voltar")
            
            try:
                opcao = int(input("Escolha uma opção: "))

            except ValueError:
                print('Erro: Digite apenas números!')
                continue

            if opcao == 1:

                registrar(conexao, "Solicitação")

            elif opcao == 2:

                listar(conexao, "Solicitação")

            elif opcao == 3:

                pesquisar(conexao, "Solicitação")

            elif opcao == 4:

                atualizar(conexao, "Solicitação")

            elif opcao == 5:

                remover(conexao, "Solicitação")

            elif opcao == 6:
                break
    
    elif opcao == 6:
        relatorio(conexao)


    elif opcao == 7:
        print("Saindo do sistema. Obrigado por utilizar nossos serviços!")
        break

    else:
        print("Opção inválida. Por favor, escolha uma opção válida.")

encerrarConexao(conexao)